name: meu_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.1.0
  permission_handler: ^11.2.0
  path_provider: ^2.0.12
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  flutter_native_splash: ^2.3.9

  flutter_compass: ^0.8.0
  latlong2: ^0.9.0
  #location: ^5.0.3
  flutter_map_animations: ^0.5.0
  google_maps_flutter: ^2.5.0
  flutter_config: ^2.0.0
  google_maps_utils: 2.0.0+1
  google_maps_controller: ^1.0.0
  wakelock: ^0.6.0
  #flutter_polyline_points: ^0.2.6
  http: ^1.2.0
  firebase_auth: ^4.16.0
  firebase_core: ^2.24.2
  shared_preferences: ^2.2.2
  firebase_storage: ^11.6.0
  image_picker: ^1.0.7
  cloud_firestore: ^4.14.0
  image_cropper: ^5.0.1
 # flutter_font_icons: ^5.0.0
  rounded_expansion_tile: ^0.0.13
  cached_network_image: ^3.3.1
  social_media_flutter: ^0.0.2
  url_launcher: ^6.2.4
  url_launcher_android: ^6.2.2
  font_awesome_flutter: ^10.6.0
  flutter_launcher_icons: ^0.13.1
  google_fonts: ^6.1.0
  responsive_builder: ^0.4.3
  local_auth: ^2.1.8
  flutter_svg: ^2.0.9
  typed_data: ^1.3.2
  adaptive_theme: ^3.6.0
  #i1ntl: ^0.19.0
  #flutter_bloc: ^7.1.0
  



  
flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/images/app/splash.png"
  android: true
  ios: true



flutter_icons:
  image_path: "assets/images/app/icon.png"
  ios: true
  remove_alpha_ios: true
  android: "launcher_icon"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/app/icon-android.png"

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    
    - assets/images/user_map.png
    - assets/images/smctic.png
    - assets/images/frente-bus_02.png
    - assets/images/bus.png
    - assets/images/Bus.png    
    - assets/images/bus_icon.png
    - assets/images/bus_color.png
    - assets/images/bus_red.png
    - assets/images/ponto.png
    - assets/images/itaguai.png
    - assets/images/user.jpg
    - assets/images/meio_brasão.png
    - assets/images/FundoAzulApp.jpg
    - assets/images/FundoAzulApp.png
    - assets/images/caminhao_lixo_icon.png
    - assets/images/pontos/Policlinica.jpeg
    - assets/images/pontos/formigao.jpg
    - assets/images/pontos/biaestampa.jpg    
    - assets/images/pontos/w.jpeg
    
    - assets/images/icons/trash.png    
    - assets/images/icons/icon_brasao.png
    - assets/images/icons/trash.png
    - assets/images/icons/caminhao_lixo_icon.png
    - assets/images/icons/chamados_icon.png
    - assets/images/icons/feed_icon.png
    - assets/images/icons/email.png
    - assets/images/icons/local.png
    - assets/images/icons/change_password_icon.png
    - assets/images/icons/profile_icon.png
    - assets/images/icons/exit_icon.png
    - assets/images/icons/sun_icon.png
    - assets/images/icons/gota_icon.png
    - assets/images/icons/air_icon.png
    - assets/images/icons/back_icon.png
    - assets/images/icons/telefone.png
    - assets/images/icons/whatsapp.png
    - assets/images/icons/bus_alert.png
    - assets/images/icons/sunny_snowing.png
    - assets/images/icons/show_modal_button.png
    - assets/images/icons/icon_brasao_color.png
    - assets/images/icons/defesa_civil.png
    - assets/images/icons/defesa_civil_icon.png
    - assets/images/icons/instagram_icon.png
    - assets/images/icons/camera01.png
    - assets/images/icons/camera02.png
    - assets/images/icons/camera03.png
    - assets/images/icons/expandir.png
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/
    - assets/images/icons/


    - assets/images/app/icon-android.png 
    - assets/images/app/splash.png



  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages